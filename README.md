# SAKKASAN Japanese writing assistant

## Installing and running

```bash
# create venv (first time only)
python3 -m venv .venv
# activate, install, run
source .venv/bin/activate
pip install -r backend/requirements.txt
python3 -m backend
```